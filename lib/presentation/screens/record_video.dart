import 'package:flutter/material.dart';

class RecordVideo extends StatelessWidget {
  const RecordVideo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("recorc video"),
    );
  }
}
